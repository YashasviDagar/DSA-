#include <bits/stdc++.h>
using namespace std;
int main(){
  // int n;                      // REVERSE OF A NUMBER
  // cin>>n;                     //PALINDROME :: since we are dividing n, make a original copy of n and check condition!
  // int temp=n;
  // int rev=0;
  // while(n>0){
  //   rev = rev*10 + n%10;
  //   n=n/10;
  // }
  // cout<<rev<<endl;
  // if(temp==rev){
  //   cout<<"palin"<<endl;;
  // }else if(temp!=rev){
  //   cout<<"not palin";
  // }

  // int n;                      // ARMSTRONG NUMBER for 3 digit num
  // cin>>n;
  // int t=n;
  // int sum=0;
  // while (n>0){
  //   int l = n%10;
  //   int c = l*l*l;
  //   sum += c;
  //   n= n/10;
  // }
  // if(t==sum) cout<<"armstrong"<<endl;
  // else cout<<"not armstorng"<<endl;

  // int n;                      //PRINTING DIVISORS USING VECTOR & SQRT!
  // cin>>n;
  // vector<int> ls;
  // for(int i=1;i<=sqrt(n);i++){
  //     if(n%i==0){
  //       ls.push_back(i);
  //       if((n/i)!=i){
  //         ls.push_back(n/i);
  //       }
  //     }
  // }
  // sort(ls.begin(),ls.end());
  // for(auto it : ls) cout<<it<<" "<<endl;
  
  return 0;
}